// @author ${user}
// @date ${date}
(function () {
	'use strict';

	app.factory('${name?replace("-", " ")?capitalize?replace(" ", "")?replace("Service", "")?replace("Factory", "")}', ${name?replace("-", " ")?capitalize?replace(" ", "")?replace("Service", "")?replace("Factory", "")});

	${name?replace("-", " ")?capitalize?replace(" ", "")?replace("Service", "")?replace("Factory", "")}.$inject = [];

	function ${name?replace("-", " ")?capitalize?replace(" ", "")?replace("Service", "")?replace("Factory", "")}() {
		return{

		};
	}
})();